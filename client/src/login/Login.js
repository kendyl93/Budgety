import React from 'react';
import GoogleButton from './GoogleButton';

const Login = () => (
  <div className="login-wrapper">
    <h1>Budgety</h1>
    <p>In order to use the app, please log in.</p>
    <GoogleButton />
  </div>
);

export default Login;
