import React from 'react';

const Login = () => (
  <div className="container">
    <h1>
      <small>In order to use the app you have to log in.</small>
    </h1>

    <div>
      <a href="http://localhost:5470/sign-in">Sign in</a>
    </div>
  </div>
);

export default Login;
